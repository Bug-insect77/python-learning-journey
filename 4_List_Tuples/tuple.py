#tuple is an immutable data type in python

a=()
b=(1,"tushar",False,1.20)

print(b)
#tuple mehods 
#Tuples are immutable — you cannot modify them after creation.As a result, there are only two built-in methods for tuples
#count(item) # count (1) will return number of times 1 occurs in tuple.
#index(item) #will return the index of first occurrence of 1 in tuple


#common methods in tuple and list
'''
len()	Returns number of items.
max()	Returns maximum element (if comparable).
min()	Returns minimum element.
sum()	Returns sum of elements (if numeric).
sorted()	Returns a new sorted list (doesn’t modify original).
'''